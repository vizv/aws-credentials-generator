{
  "name": "aws-credentials-generator",
  "description": "AWS-flavoured credentials generator",
  "devDependencies": {
    "@types/node": "^10.0.0",
    "ts-node": "^10.4.0",
    "typescript": "^3.7.7"
  },
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "scripts": {
    "start": "ts-node index.ts",
    "prebuild": "rm -rf dist",
    "build": "tsc",
    "prepack": "npm run build",
    "postpack": "rm -rf dist"
  },
  "types": "./dist/index.d.ts",
  "version": "1.0.0",
  "license": "WTFPL"
}
